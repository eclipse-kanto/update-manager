{
    "log": {
        "logFile": "/var/log/update-manager/update-manager.log"
    },
    "domain": "device",
    "agents": {
        "containers": {
            "rebootRequired": false,
            "readTimeout": "30s"
        }
    }
}